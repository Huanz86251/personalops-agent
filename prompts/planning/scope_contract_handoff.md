下面的 ScopeContract 已由独立范围解析器生成并由 Harness 完成结构校验。结构合法只说明字段、集合引用和类型有效，不保证模型的语义解释一定正确。规划时先对照用户原始请求：用户原话是最终依据，合同用于明确操作对象、条件归属和集合关系；若二者冲突，以用户原话修正Step，并在execution_guidance说明。不要扩大、缩小或改写合同中的对象范围，除非是在纠正它与用户原话之间有明确依据的冲突。若 ambiguity=true，在 Step 的 execution_guidance 中明确写出本轮采用的解释；若现有只读信息仍不能唯一确定，只安排只读确认，确认前不要执行写入。target_selection 必须表达最终采用的那一种范围，不能同时混入两个 alternatives。合同不包含 API；只能根据真实工具、Skill 或文档决定接口。

必须把 ScopeContract.effect_mode 原样复制进 Step.target_selection。constraints、required_context和resolved_time_ranges中已经明确的语义及其applies_to_sets/used_for_sets分支绑定都要保留，让执行者有字段可读，不得压缩成需要重新猜测的一句话。execution_guidance要求执行者先取得这些前置信息。READ_ONLY 只规划读取和结案；MUTATION 才规划写入、回读验收和结案。

required_context或resolved_time_ranges中的resolution_status也必须原样保留。EXPLICIT和DERIVED可作为已解析的范围依据；INFERRED只是待验证假设，UNKNOWN表示仍缺必要事实。遇到INFERRED或UNKNOWN时，在execution_guidance中先安排只读核实，并把核实结果写入验收条件；核实完成前不得据此扩大、缩小或写入目标集合。resolution_status为null只表示旧合同兼容，不得自行补猜。
