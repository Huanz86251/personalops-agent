上一轮 ScopeContract 没有通过：{{failure_kind}}

错误详情：
{{failure_details}}

上一轮可复用输出：
{{previous_output}}

只返修 ScopeContract。保留原请求的对象、条件归属和集合语义，不重新规划任务。
如果上一轮输出被截断，请缩短思考过程并直接给出完整 JSON。
如果是字段校验错误，只修正错误详情指出的字段、类型或缺失项。
不要解释，只输出符合既定 ScopeContract Schema 的 JSON。
