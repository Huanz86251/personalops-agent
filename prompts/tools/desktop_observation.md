只有用户明确要求查看或截取当前桌面时，才调用 capture_desktop_screenshot。默认保存到一个新的 `/artifacts/*.jpg` 路径；不要持续截图、后台连拍或覆盖已有文件。

如果任务只需要保留截图，创建成功后报告 artifact 路径。如果需要理解屏幕文字，把该路径交给 ocr_image；OCR 适合文字和常规界面，不等于理解任意图标、画布或复杂空间关系。

截图保存在当前任务状态，不等于已经发送到飞书。不得把虚拟 `/artifacts` 路径伪装成宿主绝对路径交给 send_local_file_to_feishu。只有经过现有交付流程得到宿主文件、且文件导出已配置并由用户逐次确认后，才能真正上传。
